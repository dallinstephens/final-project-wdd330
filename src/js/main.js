import { loadHeaderFooter } from "./utils.mjs";
// This code below we initially used, but we changed to main index.html
// to have the 4 categories instead of a product list page in the main index.html.
// import productList from "./productList.mjs";

loadHeaderFooter();
// This code below we initially used, but we changed to main index.html
// to have the 4 categories instead of a product list page in the main index.html.
// productList(".product-list", "tents");
